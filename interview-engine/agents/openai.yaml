interface:
  display_name: "Interview Engine"
  short_description: "Refine inputs with structured questions and a compact output. Use to ask user questions to fully understand a task."
  default_prompt: "Use $interview-engine to refine this request by asking the highest-leverage clarifying questions in strict JSON. Stop when you have a complete understanding."

policy:
  allow_implicit_invocation: true
