interface:
  display_name: "Interview Prompt Builder"
  short_description: "Turn the compact interview snapshot into prompt files"
  default_prompt: "Use $interview-prompt-builder to turn the current `interview_output.json` into `prompt-<yyyy-MM-dd_HH-mm-ss>.md`."

policy:
  allow_implicit_invocation: true
